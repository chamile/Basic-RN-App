import React from 'react';
import {Text, SafeAreaView} from 'react-native';

const Profile = props => {
  // console.log(props.route.params.id);
  return (
    <SafeAreaView>
      <Text>Profile SCFREEN </Text>
    </SafeAreaView>
  );
};

export default Profile;
