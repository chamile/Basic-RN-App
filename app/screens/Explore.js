import React from 'react';
import {Text, SafeAreaView} from 'react-native';

const Explore = props => {
  // console.log(props.route.params.id);
  return (
    <SafeAreaView>
      <Text>Explore SCFREEN </Text>
    </SafeAreaView>
  );
};

export default Explore;
